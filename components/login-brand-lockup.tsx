import { PartnerBrandLockup } from "@/components/partner-brand-lockup";

/** Lockup do login: Flow Focus + HubOn na barrinha. */
export function LoginBrandLockup() {
  return <PartnerBrandLockup tone="on-light" className="login-brand-lockup" />;
}
