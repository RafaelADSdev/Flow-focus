import { PageLoading } from "@/components/page-loading";

export default function RoletasLoading() {
  return (
    <PageLoading
      variant="roletas"
      label="Carregando roletas…"
      description="Montando o mapa de permissões por corretor."
    />
  );
}
